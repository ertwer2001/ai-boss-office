import {chromium} from '@playwright/test';
const browser=await chromium.launch({channel:'msedge',headless:true});const page=await browser.newPage({viewport:{width:1440,height:1000}});page.on('console',m=>console.log('console',m.type(),m.text()));page.on('pageerror',e=>console.log('error',e.message));page.on('response',async r=>{if(r.url().endsWith('/api/plan'))console.log('plan',r.status(),await r.text())});
await page.goto('http://127.0.0.1:4317');await page.getByLabel('老闆指令').fill('開發計算器然後撰寫文案');await page.getByRole('button',{name:'預覽派工',exact:true}).click();
await page.screenshot({path:'docs/screenshots/debug.png',fullPage:true});console.log(await page.locator('.toast').innerText());console.log(await page.locator('dialog').count());console.log(await page.locator('form.cmd').evaluate((f:any)=>({valid:f.checkValidity(),invalid:[...f.querySelectorAll(':invalid')].map((e:any)=>({tag:e.tagName,type:e.type,name:e.getAttribute('aria-label'),value:e.value,message:e.validationMessage}))})));await browser.close();
