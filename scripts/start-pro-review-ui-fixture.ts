import fs from 'node:fs';import path from 'node:path';import {spawn} from 'node:child_process';import {makeCompany,type Project,type Task,type Store} from '../src/domain/company';import {saveFile} from '../server/workspace';
const root=path.resolve('docs/verification/pro-ui'),data=path.join(root,'state'),out=path.join(root,'outputs');fs.mkdirSync(data,{recursive:true});
const co=makeCompany('Pro 修正介面驗證','studio',[]),p:Project={id:'candidate-project',companyId:co.id,mode:'goal',title:'計數器｜待操作驗收（隔離測試）',goal:'製作計數器',status:'blocked',blockReason:'validation',managerId:co.employees[0].id,managerModel:'test',managerEffort:'low',createdAt:new Date().toISOString(),authorizedBy:'test',calls:4,maxCalls:18,maxRevisions:2,assumptions:[],message:'作品草稿已完成，但必要操作尚未驗收，不列為正式完成。',candidateDelivery:{summary:'計數器草稿',howToUse:['開啟後按加一'],limitations:['尚未自動驗收'],entryTaskId:'draft-task',entryPath:'index.html'}};
const content='<html lang="zh-Hant"><meta charset="utf-8"><h1>待驗收計數器</h1><button onclick="document.querySelector(\'output\').textContent=Number(document.querySelector(\'output\').textContent)+1">加一</button><output>0</output></html>',rel='workspaces/draft-task/draft/v1/index.html';
const t:Task={id:'draft-task',companyId:co.id,employeeId:co.employees[1].id,projectId:p.id,kind:'work',title:'製作計數器',command:p.goal,dependsOn:[],state:'done',stage:'文字審查通過，等待操作驗收',model:'test',effort:'low',createdAt:new Date().toISOString(),logs:[],files:['程式/partial.html'],sources:[],pendingFiles:[{path:rel,sha256:saveFile(data,rel,content)}],attempt:1,reviewedAttempt:1,external:false};
const s:Store={version:1,boss:'test',companies:[co],tasks:[t],projects:[p],paused:true,maxTasksPerDay:30};fs.writeFileSync(path.join(data,'company.json'),JSON.stringify(s));
const child=spawn(process.execPath,['--import','tsx','server/index.ts'],{cwd:process.cwd(),env:{...process.env,BOSS_PORT:'4321',BOSS_DATA_DIR:data,BOSS_OUTPUT_DIR:out,BOSS_DISABLE_INFERENCE:'1'},windowsHide:true,detached:true,stdio:'ignore'});child.unref();console.log(JSON.stringify({pid:child.pid,url:'http://127.0.0.1:4321'}));
